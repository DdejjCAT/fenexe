# FENEXE Python Project Builder

Создает автономные EXE файлы из Python проектов. Всё в одном EXE: код, зависимости, Python интерпретатор.

## 🚀 Использование

### Создание проекта (быстро):
1. Поместите `builder.exe` в папку с `main.py`
2. Запустите: `builder.exe путь_к_папке_с_main.py`
3. Получите `main.exe`

### Создание билдера (для проверки на безопасность):
1. Положите в папку `builder.py` и `Vbs_To_Exe.exe`
2. Запустите: `python builder.py`
3. Получите `builder.exe`

## ⚠️ ВАЖНО: Антивирусы и VirusTotal

**VirusTotal и антивирусы могут показывать ложные срабатывания!** Это происходит потому что:

1. EXE создаётся динамически
2. Встраивается Python интерпретатор  
3. Используются VBS скрипты внутри EXE
4. EXE распаковывает файлы при запуске

**Как убедиться в безопасности:**
- **Соберите из исходников** - весь код открыт в `builder.py`
- **Проверьте код самостоятельно** - никаких скрытых функций
- **Используйте доверенный Vbs_To_Exe.exe**
- **Запускайте в песочнице/виртуалке** для тестирования

Проект создан в образовательных целях. Автор: @error_kill (Telegram/Discord).

---

## 📁 Структура собираемого проекта

```
проект/
├── main.py # Главный скрипт
├── requirements.txt # Зависимости (опционально)
├── другие файлы/ # Любые файлы проекта
└── builder.exe # Билдер (скопируйте сюда по желанию)
```

